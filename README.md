# LLM Engineer plus Python playground
## Setup environment
- Create `pyproject.toml` with dependencies
- Checkout `uv` update by `uv self update`
- Then sync `uv sync`
## API key
- Create `.env` with api key
